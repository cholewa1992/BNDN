<!-- Author
    Jacob B. Cholewa (jbec@itu.dk)
-->
@model ArtShare.Models.AccountModel

@{
    ViewBag.Title = "Edit " + Model.Username;
}

<h2>Edit Account</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()


    <div class="col-md-5">
        <h4>@Html.DisplayFor(model => model.Username)</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.Username)

        <div class="form-group">
            <h4>@Html.DisplayNameFor(model => model.Password)</h4>
            @Html.TextBoxFor(m => m.Password, new { type = "password", placeholder = "Password", @class = "form-control" })
        </div>

        <div class="form-group">
            <h4>@Html.DisplayNameFor(model => model.Email)</h4>
            @Html.TextBoxFor(m => m.Email, new { placeholder = "Email", @class = "form-control" })
        </div>

        <div class="form-group">
            <h4>@Html.DisplayNameFor(model => model.Firstname)</h4>
            @Html.TextBoxFor(m => m.Firstname, new { placeholder = "Firstname", @class = "form-control" })
        </div>

        <div class="form-group">
            <h4>@Html.DisplayNameFor(model => model.Lastname)</h4>
            @Html.TextBoxFor(m => m.Lastname, new { placeholder = "Lastname", @class = "form-control" })
        </div>

        <div class="form-group">
            <h4>@Html.DisplayNameFor(model => model.Location)</h4>
            @Html.TextBoxFor(m => m.Location, new { placeholder = "Location", @class = "form-control" })
        </div>

        <hr />

        @if (!Model.isAdmin) { 
            <div class="form-group">
                <h4>Current Password</h4>
                @Html.TextBoxFor(m => m.CurrentPassword, new { type = "password", placeholder = "Current Password", required = "true", @class = "form-control" })
                <span class="danger">@Html.ValidationMessageFor(m => m.CurrentPassword)</span>
            </div>
        }

        <div class="to-right">
            <button type="button" class="btn btn-default" onclick="location.href='@Url.Action("Details", new {Id = Model.Id})'">Cancel</button>
            <button type="submit" class="btn btn-success">Save</button>
        </div>
    </div>
    

}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
